import { Inter, Cormorant, Mulish } from "next/font/google";

export const inter = Inter({subsets: ['latin']});

export const cormorant = Cormorant({
    weight: ['300','400',],
    subsets: ['latin'],
    style: ['italic','normal']
});

export const mulish = Mulish({
    weight:['200', '300', '400', '700'],
    subsets: ['latin']
});


